"use client";import{useState}from"react";import{Check,LoaderCircle}from"lucide-react";
export function RsvpForm(){const[name,setName]=useState(""),[phone,setPhone]=useState(""),[response,setResponse]=useState("sim"),[saving,setSaving]=useState(false),[done,setDone]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setSaving(true);const r=await fetch("/api/rsvp",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name,phone,response})});if(r.ok)setDone(true);setSaving(false)}if(done)return <div className="success"><Check/><h2>Resposta registrada!</h2><p>{response==="sim"?"Que alegria! Esperamos você.":"Sentiremos sua falta, mas agradecemos por avisar."}</p></div>;return <form className="rsvp-form" onSubmit={submit}><label>Seu nome<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Nome completo"/></label><label>Seu WhatsApp<input required value={phone} onChange={e=>setPhone(e.target.value)} placeholder="(00) 00000-0000"/></label><fieldset><legend>Você poderá comparecer?</legend><label><input type="radio" checked={response==="sim"} onChange={()=>setResponse("sim")}/>Sim, estarei presente</label><label><input type="radio" checked={response==="nao"} onChange={()=>setResponse("nao")}/>Não poderei comparecer</label></fieldset><button disabled={saving}>{saving?<LoaderCircle/>:<Check/>}Confirmar resposta</button></form>}
